<div class="tropical-backdrop" aria-hidden="true">
	<div class="leaf leaf-one"></div>
	<div class="leaf leaf-two"></div>
	<div class="leaf leaf-three"></div>
	<div class="leaf leaf-four"></div>
	<div class="animal animal-dog">
		<div class="ear ear-left"></div>
		<div class="ear ear-right"></div>
		<div class="dog-face"><span class="dog-patch"></span><span class="eye eye-left"></span><span class="eye eye-right"></span><span class="muzzle"></span></div>
	</div>
	<div class="animal animal-cat"><span class="cat-ear cat-ear-left"></span><span class="cat-ear cat-ear-right"></span><span class="cat-face"><span class="cat-eye cat-eye-left"></span><span class="cat-eye cat-eye-right"></span><span class="cat-nose"></span></span></div>
	<div class="animal animal-iguana"><span class="iguana-spines"></span><span class="iguana-body"></span><span class="iguana-head"><span class="iguana-eye"></span></span><span class="iguana-tail"></span></div>
</div>

<style>
	.tropical-backdrop {
		position: fixed;
		inset: 0;
		z-index: 0;
		overflow: hidden;
		pointer-events: none;
	}

	.leaf {
		position: absolute;
		width: 9rem;
		height: 22rem;
		border-radius: 100% 0 100% 0;
		background: linear-gradient(90deg, rgba(27, 112, 39, 0.06), rgba(8, 148, 70, 0.2));
		transform-origin: bottom center;
	}

	.leaf::after {
		content: '';
		position: absolute;
		left: 50%;
		top: 0;
		width: 2px;
		height: 100%;
		background: rgba(13, 94, 43, 0.17);
		transform: rotate(-18deg);
	}

	.leaf-one { left: -3rem; top: 8rem; transform: rotate(28deg); }
	.leaf-two { right: -4rem; top: 13rem; transform: rotate(-28deg) scaleX(-1); }
	.leaf-three { left: 7%; bottom: -13rem; transform: rotate(-42deg) scale(0.8); }
	.leaf-four { right: 8%; bottom: -15rem; transform: rotate(42deg) scale(0.75) scaleX(-1); }

	.animal {
		position: absolute;
		opacity: 0.9;
	}

	.animal-dog { right: 2.5rem; bottom: 2.5rem; width: 5rem; height: 4.5rem; transform: rotate(-5deg); }
	.dog-face { position: absolute; inset: 0.4rem 0.65rem 0.15rem; border-radius: 48% 48% 44% 44%; background: #24201f; }
	.dog-face::after { content: ''; position: absolute; left: 1.5rem; bottom: -0.2rem; width: 2rem; height: 1.6rem; border-radius: 45%; background: #bd8058; }
	.ear { position: absolute; top: 0; width: 1.5rem; height: 2.8rem; border-radius: 60% 40% 50% 50%; background: #171515; }
	.ear-left { left: 0; transform: rotate(28deg); }
	.ear-right { right: 0; transform: rotate(-28deg); }
	.dog-patch { position: absolute; right: 0.55rem; top: 0.3rem; width: 1.2rem; height: 1.7rem; border-radius: 60%; background: #a86b4d; transform: rotate(25deg); }
	.eye { position: absolute; top: 1.6rem; width: 0.32rem; height: 0.32rem; border-radius: 50%; background: #f7d78a; }
	.eye-left { left: 1.15rem; } .eye-right { right: 1.15rem; }
	.muzzle { position: absolute; left: 1.45rem; bottom: 0.45rem; width: 2rem; height: 1rem; border-radius: 50%; background: #d59a70; }
	.muzzle::after { content: ''; position: absolute; left: 0.72rem; top: 0.2rem; width: 0.55rem; height: 0.35rem; border-radius: 50%; background: #201b1a; }

	.animal-cat { left: 2rem; bottom: 3rem; width: 3.8rem; height: 3.7rem; transform: rotate(7deg); }
	.cat-face { position: absolute; inset: 0.4rem 0.3rem; border-radius: 50%; background: #e7ad61; }
	.cat-ear { position: absolute; top: 0; width: 1.35rem; height: 1.5rem; background: #d18d4d; clip-path: polygon(0 100%, 25% 0, 100% 100%); }
	.cat-ear-left { left: 0.3rem; transform: rotate(-13deg); } .cat-ear-right { right: 0.3rem; transform: rotate(13deg); }
	.cat-eye { position: absolute; top: 1.45rem; width: 0.22rem; height: 0.45rem; border-radius: 50%; background: #2e462e; }
	.cat-eye-left { left: 1rem; } .cat-eye-right { right: 1rem; }
	.cat-nose { position: absolute; left: 1.62rem; top: 2rem; width: 0.4rem; height: 0.3rem; border-radius: 50%; background: #7e493f; }

	.animal-iguana { right: 10%; top: 6rem; width: 7rem; height: 3rem; transform: rotate(-9deg); }
	.iguana-body { position: absolute; left: 1.2rem; top: 0.8rem; width: 4.5rem; height: 1.7rem; border-radius: 55%; background: #72a95b; }
	.iguana-head { position: absolute; left: 0.3rem; top: 0.65rem; width: 1.8rem; height: 1.5rem; border-radius: 55% 45% 45% 55%; background: #86b968; }
	.iguana-eye { position: absolute; right: 0.3rem; top: 0.35rem; width: 0.3rem; height: 0.3rem; border-radius: 50%; background: #253b25; }
	.iguana-tail { position: absolute; right: -0.3rem; top: 1.05rem; width: 2.2rem; height: 0.6rem; border-radius: 50%; background: #5f944e; transform: rotate(-20deg); }
	.iguana-spines { position: absolute; left: 1.2rem; top: 0.2rem; width: 4.5rem; height: 0.8rem; background: repeating-linear-gradient(90deg, transparent 0 0.35rem, #4f8d48 0.36rem 0.55rem, transparent 0.56rem 0.8rem); clip-path: polygon(0 100%, 5% 0, 12% 100%, 20% 0, 28% 100%, 36% 0, 44% 100%, 52% 0, 60% 100%, 68% 0, 76% 100%, 84% 0, 92% 100%, 100% 0, 100% 100%); }

	@media (max-width: 760px) {
		.leaf { opacity: 0.65; transform: scale(0.72); }
		.animal-dog { right: 0.5rem; bottom: 1rem; transform: scale(0.72) rotate(-5deg); }
		.animal-cat { left: -0.4rem; bottom: 1rem; transform: scale(0.62) rotate(7deg); }
		.animal-iguana { right: -0.5rem; top: 9rem; transform: scale(0.65) rotate(-9deg); }
	}
</style>
